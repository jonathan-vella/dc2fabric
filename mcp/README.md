# MCP Servers — Moved

The MCP servers have been consolidated under `tools/mcp-servers/`:

| Old Path | New Path |
|----------|----------|
| `mcp/azure-pricing-mcp/` | `tools/mcp-servers/azure-pricing/` |
| `mcp/drawio-mcp-server/` | `tools/mcp-servers/drawio/` |

This directory will be removed in a future cleanup.
