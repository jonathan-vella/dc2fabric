# Schemas — Moved

JSON schema files have been consolidated under `tools/schemas/`:

```text
tools/schemas/
  agent-registry.schema.json
  explorer-graph.schema.json
  governance-constraints.schema.json
  lesson-log.schema.json
  model-catalog.schema.json
  session-state.schema.json
  workflow-graph.schema.json
```

This directory will be removed in a future cleanup.
